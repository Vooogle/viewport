{
  "name": "viewport",
  "version": "1.0.0",
  "description": "Open-source web-based video editor. CapCut-like, more advanced, plugin support.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "dev:tauri": "vite --mode tauri",
    "build": "tsc --noEmit && vite build",
    "build:tauri": "tsc --noEmit && vite build --mode tauri",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "preact": "^10.23.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.2",
    "vite": "^5.4.6"
  }
}
